{
  "name": "ranktracker-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that backs the PDQ Rank Tracker — KV-backed /data GET+PUT with shared-secret auth.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
